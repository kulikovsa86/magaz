= render 'breadcrumb'

.panel.panel-default
  .panel-heading
    %h4.panel_header= @property_group.name
  .panel-body
    = render 'form'

= link_to t('helpers.link_to.back'), property_groups_path(@parent_group)
