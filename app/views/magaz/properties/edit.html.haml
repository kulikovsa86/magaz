- if notice
  .alert.alert-success= notice

.panel.panel-default
  .panel-heading
    %h4.panel_header= @property.name
  .panel-body
    = render 'form'

%br
- if !new_property_page? && @property.type && @property.type.code == '01'
  = render 'options'


= link_to t('helpers.link_to.back'), properties_path
